This file has been consolidated to avoid drift between two divergent copies.

The single canonical App Review Notes source is:

- `docs/app-review-notes.md`

That is the file whose content gets pasted into App Store Connect's
"App Review Information > Notes" field before submission (see
`docs/ai-work/tasks/app-store-yeniden-gonderim/asc-reply-draft.md`).

Do not maintain a second copy here — edit `docs/app-review-notes.md` instead.

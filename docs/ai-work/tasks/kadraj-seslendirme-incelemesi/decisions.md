# Karar Kaydı

| Tarih | Sahip | Karar | Gerekçe | Durum |
| --- | --- | --- | --- | --- |
| 2026-07-26 | claude (sentezleyici) | Yön sesi (spatial audio) UI açıklaması ile gerçek davranışı arasındaki çelişki (yüz solda iken ses sağ kulaktan geliyor, ama ayar ekranı "sesin yüzün bulunduğu taraftan geldiğini" söylüyor) kod ve `Localizable.xcstrings`den doğrulandı, en yüksek öncelikli bulgu olarak işaretlendi | Kör kullanıcı için erişilebilirlik ayarları ekranındaki açıklama ile gerçek ses yönü ters; PROTOCOL.md'nin erişilebilirlik-riski temkinli-sonuç kuralı gereği doğrudan uygulamaya geçilmeden kullanıcıya sunuldu | kullanıcı kararı gerekli |
| 2026-07-26 | claude (sentezleyici) | Claude'un kendi r1 görüşündeki "FrameCoach için doğrudan unit test yok" iddiası reddedildi | `Tests/VideoRecorderAppTests/FrameCoachingEngineTests.swift` ve kardeş test dosyaları mevcut ve 3-kişi/2-kişi overlap/scale-imbalance/route-seçimi/frekans-kapısını doğrudan test ediyor; Codex'in görüşü bunu doğru tespit etmişti | ret (claude r1 bulgusu düzeltildi, dosya değiştirilmedi — düzeltme yalnız synthesis.md'de) |
| 2026-07-26 | claude (sentezleyici) | isHardFrameCoachInstruction İngilizce anahtar kelimelerinden 3/5'i (`too close`, `too far`, `closer to the camera`) gerçek çeviriyle eşleşiyor, 2/5'i (`not fully in frame`, `further back`) hiç eşleşmiyor — statik olarak doğrulandı | `Localizable.xcstrings` içindeki `en` çevirileri doğrudan okunarak kontrol edildi | doğrulama gerekli → kısmen doğrulandı (statik), canlı VoiceOver etkisi hâlâ doğrulama gerekli |
| 2026-07-26 | claude (uygulayıcı) | Kullanıcı "hepsini hallet" dedi; synthesis.md'nin 8 kabul edilen bulgusu ve ilgili öneriler doğrudan uygulandı (kod değişikliği) | Kullanıcının açık onayı ("şimdi bu karar verilen tüm önerilerin eksikliklerin giderilmesi lazım hepsini hallet") `handoff.md`'ye kaydedildi | kabul, uygulandı |
| 2026-07-26 | claude (uygulayıcı) | isHardFrameCoachInstruction (keyword substring matching) tamamen kaldırıldı, yerine `FrameCoachInstructionKind` enum'una dayalı tipli kontrol getirildi | Substring eşleşmesinin İngilizce'de 2/5 kelimede kırık olduğu zaten doğrulanmıştı (yukarıdaki karar); tipli çözüm bu sınıfın hatasını kökten ortadan kaldırıyor | kabul, uygulandı |
| 2026-07-26 | claude (uygulayıcı) | "Yön sesi" ayarının erişilebilirlik açıklaması değiştirildi: sesin "yüzün olduğu taraftan" değil "hareket edilmesi gereken taraftan" geldiğini söylüyor artık; kod (ses yönü hesaplaması) değiştirilmedi, sadece metin gerçek davranışa uyduruldu | Kod incelemesiyle doğrulandı: `FrameCoachSpatialCueResolver.horizontalCue` yüz solda iken `.right` üretiyor ve bu yön sağ kulaktan çalınıyor — yani mevcut davranış zaten konuşma talimatlarıyla ("biraz sağa geç") tutarlı, yanlış olan yalnızca UI metniydi | kabul, uygulandı |
| 2026-07-26 | claude (uygulayıcı) | `Localizable.xcstrings`teki "Yüz algılanamıyor, kameraya bak" anahtarının `tr` yerelleştirme DEĞERİNİN gerçekte "Yüz algılanamıyor" (kısaltılmış) olduğu keşfedildi — bu, orijinal incelemede tespit edilen "3 farklı yerde 2 farklı metin" sorununun asıl kök nedeniydi (kaynak kodun kendisi tutarlıydı ama katalog çevirisi kesikti). Katalog düzeltildi (`tr` değeri anahtarla aynı yapıldı, `en` değeri de tamamlandı) | Test çalıştırmasında `testFrameCoachIgnoresBriefFaceLossBeforeSpeakingNoFace` beklenmedik şekilde eski kısa metni döndürünce kaynak koddan değil katalogdan geldiği anlaşıldı, doğrudan JSON okunarak doğrulandı | kabul, uygulandı |
| 2026-07-26 | claude (uygulayıcı) | Kullanıcının gerçek kullanımda bildirdiği "Cmd+D → 'açık' diyor ama yönlendirme gelmiyor" hatası için `prepareAnalysisPreviewIfPossible()`teki iki redundant guard (`hasRequiredPermissions`, boş kamera/mikrofon ID kontrolü) kaldırıldı; bu durumlar zaten `configureRecorder()` içinde `CaptureRecorderError` olarak fırlatılıp `report(error)` ile VoiceOver'a duyuruluyordu, erken guard'lar o kodun çalışmasını engelliyordu | Kullanıcı raporu + kod izleme ile kök neden doğrulandı: kamera modunda bile mikrofon seçili/izinli olmadan kadraj koçu sessizce başarısız oluyordu, hiçbir neden söylemiyordu | kabul, uygulandı, regresyon testiyle doğrulandı |
| 2026-07-26 | claude (uygulayıcı) | `validateRecordingReadiness()`e `requireSystemAudioReadiness` parametresi eklendi; kadraj koçu kamera-önizleme yolları (`prepareAnalysisPreviewIfPossible`, `applySelectedInputs`) artık `isSystemAudioEnabled && ekran kaydı izni yok` kontrolünü atlıyor, yalnız gerçek kayıt başlatma (`startRecordingAsync`) bu kontrolü zorunlu tutmaya devam ediyor | Kullanıcı kamera+mikrofon izinliyken, ekran kaydı izinsizken Cmd+D'ye basınca "Ekran kaydı başlatılamadı" hatası aldığını bildirdi; kadraj koçu sistem sesi/ekran kaydı hiç kullanmadığından bu kontrolün önizleme yolunda çalışması alakasız ve yanıltıcıydı | kabul, uygulandı, regresyon testiyle doğrulandı |

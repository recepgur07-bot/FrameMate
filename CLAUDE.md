# Çoklu Ajan İş Akışı

Ortak kök eksikse güvenli başlatıcıyla oluştur; ortak kök: `/Users/recepgur/ai-workflows/ortak-ogrenme/`. Proje varsa yalnız eksik olan en küçük dosya kümesini ekle, mevcut dosyayı ezme. Açılış: ortak kök, sonra `PROTOCOL.md`, `TASKS.md`, `STATUS.md`, `project-decisions.md` ve ilgili `seyir-defteri.md`. Öncelik: açık kullanıcı talimatı → proje kararı → ortak kayıt → protokol varsayılanı. `INDEX.md`/`DERSLER.md` içinde ara; başarısız yöntemi yalnız değişen koşul ve kanıtla yeniden dene ve kaydet. Erişim yoksa açıkça bildir. Düşük etki doğrudan doğrulanabilir; yüksek etki ikinci araç incelemesi ister. Ortak dosyaları yalnız koordinatör ortak kilidiyle günceller; açık arşiv bakımı veya çelişkide geçersiz veya yerine geçen kayıt için `archive` komutunu ortak kilidiyle çalıştırır ve `INDEX.md`yi yeniler. Manuel talimat kopyalama vardır; otomatik dispatch, API teslimi, daemon, outbox veya arka plan tetikleme yoktur.
